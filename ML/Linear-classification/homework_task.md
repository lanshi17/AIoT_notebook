作业：

* 通过逻辑斯蒂回归实现手写数字多分类[0~9]

  * pandas加载digits.csv数据
  * 第一列是目标值（分10类）
  * 后面的全部是数据、特征
  * train_test_split()拆分成训练数据和测试数据
* 使用逻辑斯蒂回归建模
  * 训练数据
  * 预测类别
  * 并预测测试数据的概率
* 手动计算概率（softmax函数），观察和模型计算的概率是否相同
  * 类别比较多，方程比较多
  * 每个方程，对应一个概率

天池工业蒸汽量预测：

* 使用SVR（不同核函数）支持向量机建模预测
* 使用Lasso回归建模预测
* 使用弹性网络建模预测



​	作业新需求：

* 将几种不同模型的预测值，求带权重的平均值（尝试给不同算法分配不同权重来求平均，比如弹性网络权重0.8，SVR权重0.2）

* 对比不同算法得分差异，观察模型平均效果


整理线性分类算法知识点：

* 逻辑回归
* SVM支持向量机



对LFW人脸数据，使用逻辑回归建模预测，和SVM对比差异
